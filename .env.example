# ============================================================
# Hub 环境变量配置
# ============================================================
# 复制此文件到 apps/web/.env.local 并填入实际值
# cp .env.example apps/web/.env.local
# ============================================================

# ------------------------------------------------------------
# Supabase 配置 (必需)
# ------------------------------------------------------------
# 从 Supabase Dashboard 获取: https://supabase.com/dashboard
# 项目设置 -> API -> Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# 项目设置 -> API -> anon public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# 项目设置 -> API -> service_role key (仅服务端使用，不要暴露到客户端)
# 用于 API 路由中的管理员操作
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key-here

# ------------------------------------------------------------
# GitHub OAuth (可选 - 用于 GitHub 登录)
# ------------------------------------------------------------
# 1. 在 GitHub 创建 OAuth App: https://github.com/settings/developers
# 2. 设置 Authorization callback URL 为:
#    https://your-project-id.supabase.co/auth/v1/callback
# 3. 将 Client ID 和 Client Secret 添加到 Supabase:
#    Dashboard -> Authentication -> Providers -> GitHub
#
# 注意: GitHub OAuth 配置在 Supabase Dashboard 中完成，不需要在此设置

# ------------------------------------------------------------
# 应用配置
# ------------------------------------------------------------
# 应用 URL (用于 OAuth 回调和邮件链接)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 站点名称
NEXT_PUBLIC_SITE_NAME=Hub

# ------------------------------------------------------------
# CLI 配置 (可选 - 用于 hub-cli 同步)
# ------------------------------------------------------------
# API 密钥 (用于 CLI 工具认证)
# 可以在登录后从用户设置中生成
# HUB_API_KEY=your-api-key-here

# ------------------------------------------------------------
# 开发配置 (可选)
# ------------------------------------------------------------
# 启用详细日志
# DEBUG=true

# 禁用遥测
# NEXT_TELEMETRY_DISABLED=1
